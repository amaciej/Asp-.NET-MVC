@model IEnumerable<Lech_Poznań.Models.Funckje>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Funkcja)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Funkcja)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Funkcji }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Funkcji }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Funkcji })
        </td>
    </tr>
}

</table>
